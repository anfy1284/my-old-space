// Серверная логика приложения "login" вынесена в forms/login.server.js (фабрика),
// регистрация серверного скрипта 'login.actions' и клиентского скрипта — в init.js.
// Клиент (resources/public/client.js) вызывает функции через
// callServer('login.actions', <fn>, params). Этот файл намеренно оставлен пустым.
